# Base URL of the jobs API. Baked into the build by Vite — see the Docker
# section of README.md for why this must be set at build time, not deploy
# time.
VITE_API_BASE_URL=https://develop.api.athyna.com
