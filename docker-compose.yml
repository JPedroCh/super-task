services:
  web:
    build:
      context: .
      args:
        # Build-time only — see Dockerfile. Override with:
        #   VITE_API_BASE_URL=... docker compose up --build
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-https://develop.api.athyna.com}
    ports:
      - "8080:80"
